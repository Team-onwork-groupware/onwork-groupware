# OnWork 추적성 매핑 (유스케이스 → 구현 → 검증)

문서/설계 산출물의 요구사항이 실제 코드로 구현되고 검증되었는지 대조한다.
(원본: OnWork_유스케이스_명세서, OnWork_API_명세서, OnWork_ADR, OnWork_추적성체크리스트)

## 공통 / 인증 (Phase 1)

| 항목 | 구현 | 엔드포인트 | 검증 |
|---|---|---|---|
| 로그인 (JWT 발급) | AuthService.login | POST /api/v1/auth/login | ✅ E2E |
| 토큰 갱신 | AuthService.refresh | POST /api/v1/auth/refresh | ✅ |
| 로그아웃 (블랙리스트) | TokenBlacklistService | POST /api/v1/auth/logout | ✅ |
| 내 정보 | AuthService.me | GET /api/v1/auth/me | ✅ E2E |
| RBAC 5단계 계층 | SecurityConfig.roleHierarchy | @PreAuthorize 전역 | ✅ (403 검증) |
| 로그인 5회 잠금 | LoginAttemptService | — | ✅ 코드 |
| ADR-SYS-002 (JWT 30m/7d) | JwtTokenProvider | — | ✅ |

## 인사 (Phase 2) — UC-HR-01/02/03

| 유스케이스 | 구현 | 엔드포인트 | 검증 |
|---|---|---|---|
| UC-HR-01 신규 입사 등록 | HrService.createChangeRequest(CREATE) | POST /api/v1/hr/change-requests | ✅ E2E |
| UC-HR-02 변경 요청 승인/반려 | HrService.process | PATCH /api/v1/hr/change-requests/{id}/process | ✅ E2E |
| UC-HR-03 퇴사 처리 (soft delete) | HrService.process(RESIGN) | 〃 | ✅ 코드 |
| 직원 목록/상세 (역할범위) | HrService.listEmployees/getEmployee | GET /api/v1/hr/employees[/{id}] | ✅ E2E |
| ADR-HR-001 승인 전 미반영 | PENDING 저장, 승인 시 users 반영 | — | ✅ E2E (승인 전 로그인 실패) |
| ADR-HR-002 반려 사유 필수 | process REJECT 검증 | — | ✅ 코드 |
| 자기요청 자기승인 금지 | CANNOT_SELF_APPROVE | — | ✅ 코드 |
| 감사 이력 | employee_change_histories | — | ✅ E2E (DB 확인) |

## 근태 (Phase 3) — UC-ATTENDANCE-01/02/03/04

| 유스케이스 | 구현 | 엔드포인트 | 검증 |
|---|---|---|---|
| UC-01 출퇴근 기록 (서버시각·당일1회) | AttendanceService.clockIn/Out + Asia/Seoul Clock | POST /api/v1/attendance/clock-in, PATCH /api/v1/attendance/clock-out | ✅ E2E |
| 지각/조퇴 판정 (grace) | clockIn/Out + work_anomalies | — | ✅ E2E |
| UC-02 팀 근태 이상 조회/확정 | listAnomalies/confirmAnomaly | GET /api/v1/attendance/anomalies, PATCH .../{id}/confirm | ✅ E2E (팀범위 8 vs 24) |
| UC-03 시간외 신청 | requestOvertime | POST /api/v1/attendance/overtime-requests | ✅ E2E |
| UC-04 시간외 승인 | processOvertime | PATCH /api/v1/attendance/overtime-requests/{id}/process | ✅ E2E |
| ADR-ATT-001 결근 자정 배치 | AttendanceScheduler + detectAbsences | @Scheduled(0 0 0 * * *) | ✅ E2E (22건 자동생성) |

## 휴가 (Phase 4) — UC-LEAVE-01 외

| 유스케이스 | 구현 | 엔드포인트 | 검증 |
|---|---|---|---|
| 휴가 신청 (잔여·중복 검증) | LeaveService.request | POST /api/v1/leave-requests | ✅ E2E |
| 잔여 조회 | myBalances | GET /api/v1/leave-balances/me | ✅ E2E |
| 결재함 / 승인·보류 | inbox / process | GET /api/v1/leave/inbox, PATCH .../{id}/process | ✅ E2E |
| 신청자 취소 | cancel | PATCH /api/v1/leave-requests/{id}/cancel | ✅ E2E |
| 승인자 승인휴가 취소 | cancelApproved | PATCH /api/v1/leave-requests/{id}/cancel-approved | ✅ 코드/브라우저 |
| 보상휴가 수동 부여 | grantCompLeave | POST /api/v1/leave-grants | ✅ 코드 |
| 승인 시점 차감 / 미반영 | process APPROVE → leave_balances | — | ✅ E2E (20→17→20) |
| ADR-003 대행 결재 | LeaveApprover.activeApproverId | is_absent → delegate | ✅ E2E (delegated=true) |
| 반차 0.5일 / 평일 계산 | computeDays | — | ✅ E2E |
| 잔여 변동 이력 | leave_histories (USE/CANCEL) | — | ✅ E2E |

## 결재·알림·온보딩·대시보드 (Phase 5)

| 항목 | 구현 | 엔드포인트 | 검증 |
|---|---|---|---|
| 통합 결재함 | ApprovalService.inbox/process | GET /api/v1/approvals, PATCH /api/v1/approvals/{id}/process | ✅ E2E |
| 알림 목록/읽음 | NotificationService | GET /api/v1/notifications, PATCH .../read[-all] | ✅ E2E |
| 온보딩 투어 진행 | OnboardingService | GET /api/v1/onboarding/tutorials/me, PATCH step/visibility, POST restart | ✅ E2E |
| 대시보드 위젯 집계 | DashboardService.summary | GET /api/v1/dashboard/summary | ✅ E2E |

## 0529 명세/녹음 피드백 재점검 (2026-06-01)

| 요구사항 | 현재 반영 상태 | 근거/남은 일 |
|---|---|---|
| 역할별 화면 차등 | ✅ 반영 | 직원은 `마이페이지`, 팀장은 `팀원`, HR/경영진은 `인사` 메뉴로 표시. 직원은 결재함 메뉴와 HR 관리 테이블이 숨김 |
| HR DRAFT canonical 제거 | ✅ 반영 | 프론트에서 임시저장 UI 제거, canonical API는 `POST /hr/change-requests`와 batch-process 중심 |
| HR 일괄 승인 | ◐ 부분 | API와 max 50/partial success/`batch_id` 응답 구현. `batch_id` DB 감사 컬럼 저장은 남음 |
| 근태 Asia/Seoul 시간대 | ✅ 반영 | `ClockConfig`와 `AttendanceService` Clock 주입 |
| 근태 이상 확정 세부 처리 | ◐ 부분 | 기존 confirm은 유지. `anomaly_type`, `overtime_approved` body 처리와 미인정 시 overtime 0 처리는 남음 |
| 근태 월마감 | ◐ 부분 | `/attendance/monthly-summaries` 추가. 미확정 이상자 수/월마감 잠금 수준의 상세 집계는 남음 |
| 휴가 canonical endpoint | ✅ 반영 | `/leave-balances/me`, `/leave-requests*`, `cancel-approved`, `leave-grants`, `summary` 추가 |
| 승인 휴가 신청자 직접 취소 차단 | ✅ 반영 | 백엔드 권한 오류 + 프론트 취소 버튼 숨김 |
| 결재함 상세 확인 | ✅ 반영 | 행 클릭 drawer로 유형, 요청자, 업무 ID, 대기일, 상세 내용 표시 |
| 결재함 실제 approvals 테이블 라우팅 | ◐ 부분 | canonical API는 구현. 물리 `approvals` 테이블 동기화/라우팅 인덱스 전환은 남음 |
| 온보딩 me/step/visibility/restart | ✅ 반영 | API와 홈 자동 노출/그만보기/재시작 UI 구현 |
| 성능·품질 증거 | ◐ 부분 | `./gradlew test`, `npm run lint`, `npm run build`, 브라우저 QA는 통과. p95/p99 부하 스크립트와 결과는 남음 |

## 통합 검증 (Phase 6)

| 항목 | 구현 | 검증 |
|---|---|---|
| 레이어 경계 (ADR-SYS-001) | LayeredArchitectureTest (ArchUnit) | ✅ 4규칙 통과 |
| 도메인 단위 테스트 | LeaveBalance/HrChangeRequest/LeaveApprover/DailyWorkRecord Test | ✅ 13건 통과 |
| 컨텍스트 로드 | OnWorkApplicationTests | ✅ 통과 |
| CI 자동화 | .github/workflows/ci.yml (PG16+Redis7 서비스, 스키마/시드, gradle test, 프론트 build) | ✅ 구성 |

## 미구현 / 향후 (정직한 한계)
- 첫 로그인 비밀번호 강제 변경 화면 (백엔드 임시비번=사번은 구현, 강제변경 UI 미구현)
- 근태 이상 확정 시 유형 재지정과 미승인 시간외 인정/미인정 처리
- 월마감의 미확정 이상자 집계, 마감 잠금, 보상휴가 자동 산정 배치
- 통합 결재함의 approvals 테이블 동기화(현재는 모듈별 테이블 집계 방식)
- p95/p99/TPS 성능 스크립트와 결과 리포트
